'use strict';

/**
 * physical-sizing-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::physical-sizing-card.physical-sizing-card');
