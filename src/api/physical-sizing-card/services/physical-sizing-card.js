'use strict';

/**
 * physical-sizing-card service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::physical-sizing-card.physical-sizing-card');
